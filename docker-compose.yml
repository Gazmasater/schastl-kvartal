version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cats-house-frontend
    restart: always
    ports:
      - 3002:3000
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
