export type TAboutProject = {
  heading: string
  smallText: string[]
  featureFirst: {
    label: string
    subtext: string
  }
  featureSecond: {
    label: string
    subtext: string
  }
  linkToMapProvider: string
  mapText: string
}
